// src/components/atoms/SectionTitle.jsx

const SectionTitle = ({ title }) => {
  return (
    <h2 className="text-2xl font-semibold text-gray-800 tracking-tight mb-6 border-b border-gray-200 pb-2">
      {title}
    </h2>
  );
};

export default SectionTitle;
